
<table class="table table-striped table-condensed">
	<thead>
		<tr> 
		  
		<th style="text-align:center"colspan="8"><h3>Jindal</h3></th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>Size/Brand</td>
			<% Type.all.each do |t| %>
				<td><%=t.type_name%> </td>
			<% end %>
		</tr>


		<% Size.all.each do |s| %>
			<tr>
				<td><%= s.size %></td> 
				<% Type.all.each do |type| %>
				<td><%= s.types.where(:type_name => "#{type.type_name}").count %></td>
				<% end %>
			</tr>
	 	<% end %>
		
	</tbody>

</table>
