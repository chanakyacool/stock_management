has put a stock :- 
<% if activity.trackable %>

	<b><%= activity.trackable.quantity %></b> PIPES of <b><%= Size.find(activity.trackable.size).size%></b> in <b><%= Company.find(activity.trackable.company_id).company_name %></b> of <b><%= Godown.find(activity.trackable.godown_id).godown_place %></b> Godown- <small><%= time_ago_in_words(activity.trackable.created_at)%> ago </small>
<% else %>
	<b>Which has since been removed</b>
<% end %>
